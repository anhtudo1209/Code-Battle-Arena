1. INSTALL PACKAGE

cd backend
npm install

cd frontend
npm install

2. INSTALL DOCKERFILE
docker build -t my-app .

3. .env FILE ( Backend)
PORT = ? 
JWT_SECRET = ?

DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=vipboy1478
DB_NAME=Code Battle Arena


4. .env FILE ( Frontend)
VITE_GOOGLE_CLIENT_ID = ?